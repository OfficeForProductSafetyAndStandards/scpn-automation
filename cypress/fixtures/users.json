{
    "opss": {
      "email": "${{ secrets.USER_EMAIL }}",
      "password": "${{ secrets.USER_PASSWORD }}",
      "code": "${{ secrets.TWO_FACTOR_AUTH_KEY }}",
      "rp": "${{ secrets.RP }}",
      "auth_user": "${{ secrets.BASIC_AUTH_USERNAME }}",
      "auth_password": "${{ secrets.BASIC_AUTH_PASSWORD }}"
    }
}