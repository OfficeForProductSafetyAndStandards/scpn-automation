{
  "opss": {
    "email": "${ USER_NAME }",
    "password": "${ USER_PASSWORD }",
    "code": "${ TWO_FACTOR_AUTH_KEY }",
    "rp": "${ RP }",
    "auth_user": "${ BASIC_AUTH_USERNAME }",
    "auth_password": "${ BASIC_AUTH_PASSWORD }"
  }
}
